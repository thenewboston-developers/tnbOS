import styled from 'styled-components';

import {TOTAL_LEFT_SPACING} from 'apps/Trade/components/OrderCardHeader/Styles';

export const Container = styled.div`
  margin-left: ${`${TOTAL_LEFT_SPACING}px`};
`;
