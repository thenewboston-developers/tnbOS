import {SFC} from 'system/types';
import * as S from './Styles';

const Transfers: SFC = ({className}) => {
  return <S.Container className={className}>Transfers</S.Container>;
};

export default Transfers;
