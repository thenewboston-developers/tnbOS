import {SFC} from 'system/types';
import * as S from './Styles';

const MyCollection: SFC = ({className}) => {
  return <S.Container className={className}>MyCollection</S.Container>;
};

export default MyCollection;
