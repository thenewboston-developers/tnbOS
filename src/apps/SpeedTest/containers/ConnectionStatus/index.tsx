import {SFC} from 'system/types';
import * as S from './Styles';

const ConnectionStatus: SFC = ({className}) => {
  return <S.Container className={className}>ConnectionStatus</S.Container>;
};

export default ConnectionStatus;
