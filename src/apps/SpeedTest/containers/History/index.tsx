import {SFC} from 'system/types';
import * as S from './Styles';

const History: SFC = ({className}) => {
  return <S.Container className={className}>History</S.Container>;
};

export default History;
