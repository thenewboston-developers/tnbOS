import {SFC} from 'system/types';
import * as S from './Styles';

const BuyOrders: SFC = ({className}) => {
  return <S.Container className={className}>BuyOrders</S.Container>;
};

export default BuyOrders;
