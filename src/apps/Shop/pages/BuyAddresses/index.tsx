import {SFC} from 'system/types';
import * as S from './Styles';

const BuyAddresses: SFC = ({className}) => {
  return <S.Container className={className}>BuyAddresses</S.Container>;
};

export default BuyAddresses;
