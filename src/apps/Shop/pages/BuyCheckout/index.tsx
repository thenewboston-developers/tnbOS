import {SFC} from 'system/types';
import * as S from './Styles';

const BuyCheckout: SFC = ({className}) => {
  return <S.Container className={className}>BuyCheckout</S.Container>;
};

export default BuyCheckout;
