import {SFC} from 'system/types';
import * as S from './Styles';

const SellShipping: SFC = ({className}) => {
  return (
    <S.Container className={className}>
      SellShipping
    </S.Container>
  );
};

export default SellShipping;
