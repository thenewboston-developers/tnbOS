import styled from 'styled-components';

import {TOTAL_LEFT_SPACING} from 'apps/Shop/components/OrderPaymentHeader/Styles';

export const Container = styled.div`
  margin-left: ${`${TOTAL_LEFT_SPACING}px`};
`;
