import {SFC} from 'system/types';
import * as S from './Styles';

const MyCourses: SFC = ({className}) => {
  return <S.Container className={className}>My Courses</S.Container>;
};

export default MyCourses;
