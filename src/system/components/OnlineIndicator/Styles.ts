import styled from 'styled-components';

export const Inner = styled.div`
  bottom: 0;
  display: flex;
  position: absolute;
  right: 0;
`;

export const Outer = styled.div`
  bottom: -2px;
  display: flex;
  position: absolute;
  right: -2px;
`;
