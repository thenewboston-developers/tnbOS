import {GenericFunction, GenericVoidFunction, SFC} from 'system/types/generic';
import {IdentificationData} from 'system/types/identification';
import {IpcChannel} from 'system/types/ipc';
import {SelfSlice} from 'system/types/self';
import {AccountNumber, SigningKey, TnbKeyPair} from 'system/types/signing';
import {SystemElectronStore} from 'system/types/systemElectronStore';
import {ToastType} from 'system/types/toast';

export {
  AccountNumber,
  GenericFunction,
  GenericVoidFunction,
  IdentificationData,
  IpcChannel,
  SFC,
  SelfSlice,
  SigningKey,
  SystemElectronStore,
  TnbKeyPair,
  ToastType,
};
